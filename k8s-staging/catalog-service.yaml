apiVersion: apps/v1
kind: Deployment
metadata:
  name: catalog-service
  namespace: affnet-staging
spec:
  replicas: 1
  selector:
    matchLabels:
      app: catalog-service
  template:
    metadata:
      labels:
        app: catalog-service
    spec:
      containers:
        - name: catalog-service
          image: ghcr.io/your-org/catalog-service:latest
          ports:
            - containerPort: 3000
          env:
            - name: POSTGRES_USER
              valueFrom: { secretKeyRef: { name: affnet-secrets, key: POSTGRES_USER } }
            - name: POSTGRES_PASSWORD
              valueFrom: { secretKeyRef: { name: affnet-secrets, key: POSTGRES_PASSWORD } }
          envFrom:
            - configMapRef: { name: affnet-env }
          resources:
            requests: { cpu: "100m", memory: "128Mi" }
            limits: { cpu: "300m", memory: "256Mi" }
---
apiVersion: v1
kind: Service
metadata:
  name: catalog-service
  namespace: affnet-staging
spec:
  type: ClusterIP
  selector:
    app: catalog-service
  ports:
    - name: http
      port: 3000
      targetPort: 3000

