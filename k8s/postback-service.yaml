apiVersion: apps/v1
kind: Deployment
metadata:
  name: postback-service
  namespace: affnet
spec:
  replicas: 1
  selector:
    matchLabels:
      app: postback-service
  template:
    metadata:
      labels:
        app: postback-service
    spec:
      containers:
        - name: postback-service
          image: ghcr.io/your-org/postback-service:latest
          ports:
            - containerPort: 3000
          livenessProbe:
            httpGet:
              path: /health
              port: 3000
            initialDelaySeconds: 10
            periodSeconds: 10
          readinessProbe:
            httpGet:
              path: /health
              port: 3000
            initialDelaySeconds: 5
            periodSeconds: 5
          envFrom:
            - configMapRef: { name: affnet-env }
            - secretRef: { name: affnet-secrets }
          resources:
            requests: { cpu: "50m", memory: "64Mi" }
            limits: { cpu: "300m", memory: "256Mi" }
---
apiVersion: v1
kind: Service
metadata:
  name: postback-service
  namespace: affnet
  annotations:
    prometheus.io/scrape: "true"
    prometheus.io/port: "3000"
    prometheus.io/path: "/metrics"
spec:
  type: ClusterIP
  selector:
    app: postback-service
  ports:
    - name: http
      port: 3000
      targetPort: 3000
